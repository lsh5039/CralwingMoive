<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.lsh.movie.mybatis.MovieDao"><!--이 xml의 고유한 이름을 설정MybatisMapper로  -->
 
 	
<select id="getOneMovie" resultType="com.lsh.movie.crawling.MovieVO">
	SELECT * FROM movie WHERE pk = #{pk};
</select>
 
 
  
  
 </mapper> 
